import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('ProductsAddPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ProductsAddPage(title: 'ProductsAdd')));
    //  final titleFinder = find.text('ProductsAdd');
    //  expect(titleFinder, findsOneWidget);
  });
}
