import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('ProductsListPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ProductsListPage(title: 'ProductsList')));
    //  final titleFinder = find.text('ProductsList');
    //  expect(titleFinder, findsOneWidget);
  });
}
