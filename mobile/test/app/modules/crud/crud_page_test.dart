import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('CrudPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(CrudPage(title: 'Crud')));
    //  final titleFinder = find.text('Crud');
    //  expect(titleFinder, findsOneWidget);
  });
}
